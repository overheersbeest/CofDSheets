<UserControl x:Class="CofD_Sheet_WPF.Views.Components.LabeledTextField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CofD_Sheet_WPF.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Label Name="Label" Content="{Binding Field.Label}" Grid.Column="0"/>
		<TextBox Name="Value" Text="{Binding Field.Value, UpdateSourceTrigger=PropertyChanged}" TextChanged="{Binding valueChangedEvent}" Grid.Column="1"/>
		<RadioButton />
	</Grid>
</UserControl>
